{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/format.js"],
            "timeout": 30,
            "statusMessage": "Linting and formatting..."
          }
        ]
      }
    ]
  }
}
