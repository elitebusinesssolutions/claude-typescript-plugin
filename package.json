{
  "name": "elite-typescript-plugin",
  "private": true,
  "version": "0.1.0",
  "description": "Test suite for the elite-ts Claude Code plugin's hook scripts",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "validate-skill-evals": "node scripts/validate-skill-evals.js"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "prettier": "^3.9.4"
  },
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24"
  }
}
